# Topk问题
## 大/小根堆
64 45 52 80 66 68 0 2 18 75
问：求出上面序列中值最小的前三个元素
- 遍历：O(n^2)
- 高级排序：O(nlogn)
- 怎么在O(n)线性时间内找到topk的元素？？
需要用一个大根堆 堆顶元素的值是最大的 思想：把大根堆堆顶的大值不断地淘汰，放入小值

## 快排分割
利用快排的思想求出topk数
假设partition找到的数的下标是pos
1. pos == K  -> 直接返回
2. pos > K   -> 去左边递归
3. pos < K   -> 去右边递归