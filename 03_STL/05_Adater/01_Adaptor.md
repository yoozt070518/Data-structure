# 适配器
- 对于vector，deque，list这样的我们叫它们为容器，而像stack，queue这样的 我们称之为适配器
- 原因：因为适配器做不到像容器那样直接管理物理内存 而是在体内利用容器来完成这一目标 相当于只是容器套了一个外壳


- 设计核心：**对接口限制**
    - 比如，栈的绝对铁律：后进先出，绝不云逊中间插入，也绝不允许遍历

自己不造轮子，白嫖已有容器的内存管理，通过阉割接口来实现特定的数据结构铁律。